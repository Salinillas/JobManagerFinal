<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.jobmanager">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icono_jobmanager"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icono_jobmanager"
        android:supportsRtl="true"
        android:theme="@style/Theme.JobManager"
        tools:targetApi="30">

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.JobManager.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RegisterActivity"
            android:label="@string/register_activity_label" />

        <activity
            android:name=".FicharUsuario" />

        <activity
            android:name=".MenuActivity" />

        <activity
            android:name=".OficinasActivity" />
        <activity
            android:name=".UsuariosActivity" />
        <activity
            android:name=".UserConfigActivity" />

    </application>

</manifest>